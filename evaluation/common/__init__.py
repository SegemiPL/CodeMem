"""Shared building blocks for CodeMem Harbor task generators.

Domain packages (revert_eval, feature_eval, ...) keep only their data
parsing and phase semantics; everything mechanical about scaffolding a
Harbor task lives here so new task types can be adapted quickly.
"""
